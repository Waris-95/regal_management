/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['c.tenor.com', 'icon-library.com', 's3-inventorymanagement.s3.us-east-2.amazonaws.com'], // Add the domain you want to allow
  },
};

export default nextConfig;
