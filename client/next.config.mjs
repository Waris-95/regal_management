/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['c.tenor.com', 'icon-library.com', 'bananarepublicfactory.gapfactory.com'], // Add the domain you want to allow
  },
};

export default nextConfig;
